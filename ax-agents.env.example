# ax-clawdbot Agent Configuration
#
# Copy this file to ax-agents.env and fill in your values
# Then run: ./setup.sh sync
#
# Format: AGENT_N=id|secret|handle|env
#
# Fields:
#   id     - Agent UUID from https://paxai.app/register
#   secret - Webhook secret (HMAC signing key)
#   handle - Your @handle (for logging)
#   env    - Environment label (prod/local/dev)

AGENT_1=your-agent-uuid|your-webhook-secret|@your-agent|prod

# Multi-agent example:
# AGENT_1=uuid1|secret1|@mybot|prod
# AGENT_2=uuid2|secret2|@mybot-dev|local
