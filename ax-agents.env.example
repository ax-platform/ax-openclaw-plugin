# ax-clawdbot Agent Configuration
# Copy this file to ax-agents.env and fill in your values
# Then run: ./scripts/setup.sh
#
# Format: One agent per line
# AGENT_<N>=id|secret|handle|env
#
# Example:
# AGENT_1=abc123-uuid|webhook-secret|@myagent|prod
# AGENT_2=def456-uuid|another-secret|@myagent-dev|local

AGENT_1=your-agent-uuid|your-webhook-secret|@your-agent|prod

# Optional: Your webhook URL (for reference only)
# WEBHOOK_URL=https://your-tunnel.trycloudflare.com/ax/dispatch
